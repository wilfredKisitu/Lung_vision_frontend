import 'package:flutter/material.dart';

class DiagnoseCt extends StatelessWidget {
  const DiagnoseCt({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Diagnose by CT'));
  }
}
